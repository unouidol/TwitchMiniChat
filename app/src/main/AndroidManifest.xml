<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Allows TMC to present only installed external web browsers. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.APP_BROWSER" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="http" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
    </queries>


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TwitchMiniChat">

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_notification_bell" />

        <activity
            android:name="com.fs.twitchminichat.pcg.mostwanted.PcgMostWantedActivity"
            android:exported="false"
            android:label="@string/pcg_most_wanted_title" />

        <activity
            android:name="com.fs.twitchminichat.pcg.PcgActivity"
            android:exported="false" />

        <activity
            android:name="com.fs.twitchminichat.AuthCallbackActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="${authScheme}"
                    android:host="auth" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.fs.twitchminichat.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.TwitchMiniChat"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".SafetyPrivacyActivity" />
        <activity
            android:name=".DataAccountControlActivity"
            android:exported="false"
            android:label="@string/data_account_control" />
        <activity android:name=".BlockedUsersActivity" />
        <activity android:name=".PolicyPageActivity" />

        <service
            android:name="com.fs.twitchminichat.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>
